<div class="container-fluid">
<h1>Place where the admin can do things for the rest of the users</h1>


<h6>Usuarios que necesitan autorización:</h6>

<div class="table-list">
    <table id="miTabla" class="table table-sm table-hover">
    <caption>Tabla de usuarios deshabilitados</caption>
    <thead class="thead-dark">
        <tr>
            <th scope="col">Email</th>
            <th scope="col" class="d-none d-md-table-cell">Nombre</th>
            <th scope="col" class="text-center">Fecha</th>
            <th scope="col" class="text-center">Habilitar</th>
        </tr>
    </thead>
    <tbody>
    {{#each disabledUsers}}
        <tr>
            <th>{{email}}</th>
            <th class="d-none d-md-table-cell">{{username}}</th>
            <th class="text-center">{{created}}</th>
            <th class="text-center"><a href="authorize?username={{username}}" class="btn btn-success btn-sm btn-square btn-block btn-table">OK</a></th>
        </tr>
    {{/each}}
    </tbody>
    </table>
</div>




<!-- Mostrar resultados en tabla -->
{{!-- TODO:

    * Poner un botón selector a la izquierda, y uno para seleccionar a todos. Y tener un botón de relanzamiento de trabajos.

    * Poder solicitar actualizaciones de todos, de usuarios o de listas.

    * Autorizar acceso a aplicación. Tabla con solicitudes. Igual que el primero con un selector a la izquierda y un botón para lanzar la autorización.

    * Tener algún tipo de estadísticas de movimiento por usuarios. Subida, creación de listas. 
--}}
{{!-- <h6>Trabajos en error</h6>
<div>
    <table id="miTabla2" class="table">
    <thead>
        <tr>
            <th scope="col">SongId</th>
            <th scope="col">List</th>
            <th scope="col">User</th>
            <th scope="col">State</th>
            <th scope="col">Date Last Movement</th>
        </tr>
    </thead>
    <tbody>
    {{#each works}}
        <tr>
            <th><a href="https://www.youtube.com/watch?v={{songId}}">{{songId}}</a></th>
            <th>{{listName}}</th>
            <th>{{email}}</th>
            <th>{{state}}</th>
            <th>{{date}}</th>
        </tr>
    {{/each}}
    </tbody>
    </table>
</div> --}}

</div>
